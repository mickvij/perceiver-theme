{
  "name": "perceiver-tests",
  "version": "1.0.0",
  "description": "Playwright tests voor perceiver.nl",
  "scripts": {
    "test": "playwright test",
    "test:mobile": "playwright test --project='Mobile Safari'",
    "test:desktop": "playwright test --project='Desktop Chrome'",
    "test:staging": "TEST_URL=https://test.perceiver.nl playwright test",
    "report": "playwright show-report report"
  },
  "devDependencies": {
    "@playwright/test": "^1.42.0"
  }
}
